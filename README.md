# .github

Org-wide files for [p2p-songs](https://github.com/p2p-songs):

- `profile/README.md` — rendered as the org's homepage on github.com/p2p-songs
- `docs/` — project-wide documentation (architecture, protocol spec, review checklist, audits) shared across the `player`, `addon-sdk`, `addons`, and `backend` repos

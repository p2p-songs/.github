# Player P-5 minimal app audit

- **Audit ID:** A-010
- **Status:** CHANGES REQUIRED — 1 medium
- **Supersedes:** A-009 for current implementation sign-off
- **Audited commits:** `.github` `64e49dcce5d26bfe80f543649b3732849e3e9a13`; `player` `00787ae83d35691c35e58c9972560f39f866f809`; `addon-sdk` `b9a55a7e7ea0a324da92f49ee00df70dd6e0c9c2`; `addons` `6e551b99c45fd9cea88cdb07d9dd4b6f9bc2d817`; `backend` `682adc7ed6b5db10d37db9b8a344b65b663e17f9`
- **Last updated:** 2026-07-21

## Scope and verdict

This pass audited the P-5 player slice end to end and rechecked the active
cross-repo invariants. Protocol neutrality, schema validation, legal-source
constraints, scheduler/query-plane separation, resolved-media persistence,
configured-URL redaction, provider isolation, and the A-009 fixes remain sound.
The lite end-to-end slice proves that the implemented addons compose and can
play a song. One current-scope recovery defect remains: debounced session
persistence can lose the latest queue state on close/reload.
The backend and the not-yet-built `stream-debrid`/`stream-ytmusic` components
remain out of implementation scope, so their future invariants were checked for
drift but not claimed as implemented.

## Findings

### [MEDIUM] Debounced queue persistence loses recent state on close or reload

- **Category:** end-to-end-user-value
- **Repo / file / line:** `player/src/ui/viewmodels/usePersistSession.ts:52`
- **Reference:** player ARCHITECTURE §6 and Adversarial Review Contract §2a close/reload journey
- **Finding:** Queue changes are saved only after an 800 ms timer. Cleanup clears the timer without flushing, and there is no `pagehide`/visibility lifecycle flush. Save failures are also detached without recovery or user-visible state.
- **Why it matters:** Closing or reloading within 800 ms of selecting, reordering, or replacing the queue deterministically leaves the previous durable snapshot. On the next launch the app restores stale queue identity, violating the promised restore journey and potentially losing a user's just-made queue edits.
- **Suggested fix:** Keep debouncing normal writes, but serialize/flush the latest snapshot during an appropriate page lifecycle event and on effect cleanup where the host permits it; track the latest queued write and handle persistence rejection. Add a fake-timer lifecycle test reproducing change-then-close-before-800-ms.
- **Verdict:** CONFIRMED

## Six-lens disposition

- **Technical soundness:** no finding. Core engine, protocol validation, and command/query separation pass for the implemented slice. CSP/Trusted Types becomes an implementation gate when credential-bearing addons enter scope.
- **Legal validity:** no finding. The player remains source-neutral; `stream-legal` remains fixed-source and fail-closed. Unbuilt debrid/YouTube addons were not credited as implemented.
- **Overall implementation quality:** changes required through the persistence lifecycle gap (the sole finding). Existing automated suites are green.
- **System design and operations:** no additional finding. Backend is scaffolding-only and optional; current provider boundaries, deadlines, and persistence port remain coherent.
- **UI/UX and accessibility:** no finding for the explicitly desktop, lite E2E slice. Basic semantic buttons, labels, status/alert roles, and keyboard-operable controls are present. Responsive/mobile optimization remains future polish and was not assessed as a release claim.
- **End-to-end user value and delight:** changes required; close/reload can restore stale state (the sole finding). First-launch guidance, honest playback errors, empty states, source redaction, and retry affordances are otherwise present.

## Withdrawn after scope clarification

- **Browser CSP / Trusted Types:** withdrawn as premature. No implemented addon currently carries user credentials. Checklist §7 remains a hard implementation gate before credential-bearing configured addons (notably `stream-debrid`) land.
- **Mobile layout:** withdrawn as out of scope. P-5 is an explicitly lite desktop E2E experience intended to prove addon composition and song playback; responsive UI/UX is deferred polish.

## Verification

- `player`: 172/172 tests, typecheck, and Vite production build passed (loopback permission required for the live HTTP suite).
- `addon-sdk`: 46 protocol tests + 36 SDK tests, typecheck, and build passed (loopback permission required for `serveHTTP`).
- `addons`: 46/46 tests, typecheck, and build passed.
- Repository inspection found no raw configured URL rendering, console logging, remote UI loading, service worker, or `/stream` use through TanStack Query.
